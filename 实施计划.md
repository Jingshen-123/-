# 中文语序调整器 — 实施计划

## 需求总结

| 维度 | 决定 |
|------|------|
| 技术栈 | Python + PyQt6 |
| AI 后端 | DeepSeek API |
| 触发方式 | 点击按钮处理 |
| 语言支持 | 仅中文 |
| API Key 管理 | 软件内设置页面，保存到本地配置文件 |
| 辅助功能 | 复制结果 + 清空 |
| UI 风格 | 酷炫黑色主题 |

---

## 项目结构

```
/
├── main.py                      # 入口，启动应用
├── requirements.txt             # PyQt6, openai
├── CLAUDE.md                    # AI 助手指引（指向各标准文件）
├── docs/
│   ├── 需求规格说明书.md         # 功能需求、用户场景、验收标准
│   ├── 技术方案.md               # 技术选型、架构设计、模块职责
│   ├── 设计规范.md               # UI 设计规范、颜色、字体、布局
│   └── 开发步骤.md               # 分阶段开发步骤（本文档权威版本）
├── dev_logs/
│   └── YYYY-MM-DD.md            # 每日自动记录：完成事项 + 待办事项
├── ui/
│   ├── __init__.py
│   ├── main_window.py           # 主窗口
│   ├── settings_dialog.py       # API Key 设置弹窗
│   └── styles.py                # 全局黑色主题 QSS 样式
├── services/
│   ├── __init__.py
│   └── text_processor.py        # DeepSeek API 调用封装
└── config/
    ├── __init__.py
    └── settings.py              # API Key 本地持久化（JSON）
```

---

## 架构设计

### 1. 主窗口布局（main_window.py）
- **顶部**：标题栏 + 设置按钮，打开设置弹窗
- **中部上**：输入框（QTextEdit），placeholder "请输入需要润色的中文文本..."
- **中部**：操作按钮区 —— "润色"（主按钮）、"清空"
- **中部下**：输出框（QTextEdit，只读），placeholder "润色结果将显示在这里..."
- **底部**：状态栏（处理状态/错误信息）+ 复制按钮

### 2. 文本处理流程（text_processor.py）
- 使用 `openai` SDK，`base_url="https://api.deepseek.com"`
- 发送前检查 API Key 是否存在，无则提示用户先设置
- System Prompt：调整语序 + 删除啰嗦表述，保持语义完整
- 调用 `client.chat.completions.create()`，处理超时/网络错误

### 3. 设置管理（config/settings.py）
- API Key 存储在 `%APPDATA%/chinese-polisher/config.json`
- 接口：`get_api_key()` / `save_api_key(key)`
- 首次使用 key 为空，主窗口检测后引导进入设置

### 4. UI 风格（styles.py）
- 背景：深黑 #0d0d0d / #1a1a1a
- 主色调：青色 #00d4ff
- 按钮：深色底 + 亮色边框 + hover 发光
- 输入/输出框：深灰底 #1e1e1e + 浅灰文字 #cccccc + 圆角
- 字体：Microsoft YaHei

---

## 依赖

```
PyQt6>=6.5
openai>=1.0.0
```

---

## 分阶段开发步骤

每阶段完成后需验证通过，再进入下一阶段。

### 阶段一：项目初始化
- 创建目录结构（ui/、services/、config/、docs/、dev_logs/）
- 创建 requirements.txt
- 安装依赖：`pip install -r requirements.txt`
- 验证：`python -c "from PyQt6.QtWidgets import QApplication; import openai; print('OK')"`

### 阶段二：配置层
- 实现 `config/settings.py`（API Key 读写）
- 实现 `config/__init__.py`
- 验证：独立运行 settings.py，读写一个测试 key 到 %APPDATA%

### 阶段三：样式系统
- 实现 `ui/styles.py`（全局 QSS 样式字符串）
- 实现 `ui/__init__.py`
- 验证：用最小 QWidget 窗口加载样式，检查视觉效果

### 阶段四：设置弹窗
- 实现 `ui/settings_dialog.py`
- 窗口布局：标题、输入框（带 placeholder）、保存/取消按钮
- 保存后调用 config/settings.py 持久化
- 验证：独立启动弹窗，输入测试 key，保存后确认文件写入

### 阶段五：文本处理服务
- 实现 `services/text_processor.py`
- 实现 `services/__init__.py`
- 封装 `polish_text(text, api_key)` 函数
- 验证：写一个独立脚本，传入一段啰嗦中文文本，打印结果

### 阶段六：主窗口组装
- 实现 `ui/main_window.py`
- 集成输入框、输出框、按钮、状态栏、设置弹窗
- 实现按钮交互逻辑（润色、清空、复制、打开设置）
- 处理异常：无 API Key、网络错误、API 返回异常
- 验证：启动主窗口，完成完整流程测试

### 阶段七：入口与收尾
- 实现 `main.py`（QApplication 启动 + 样式加载 + 窗口显示）
- 验证：`python main.py` 启动，完整流程走通

---

## docs/ 标准文件说明

| 文件 | 内容 |
|------|------|
| 需求规格说明书.md | 功能需求清单、用户场景、验收标准 |
| 技术方案.md | 技术选型理由、模块划分、数据流 |
| 设计规范.md | 颜色、字体、间距、组件样式、交互规范 |
| 开发步骤.md | 分阶段步骤（与本文同步，为权威执行版本） |

---

## dev_logs/ 日志规范

- 每天开发开始时创建 `YYYY-MM-DD.md`
- 内容格式：
  ```
  # 开发日志 YYYY-MM-DD
  
  ## 今日完成
  - [完成项 1]
  - [完成项 2]
  
  ## 待办事项
  - [待办 1]
  - [待办 2]
  
  ## 遇到的问题
  - [问题描述及解决方案]
  ```
- 每天开发结束后更新日志

---

## CLAUDE.md 指引

CLAUDE.md 位于项目根目录，包含：
1. 项目简介
2. 标准文件路径索引（指向 docs/ 各文件）
3. 开发工作流说明（分阶段执行、每阶段验证后再继续）
4. 开发日志维护规则
5. 关键约束（不要跨阶段开发、不要跳过验证）

---

## 验证方式

1. `python main.py` 启动，窗口正常显示
2. 未设 API Key 时点击润色 → 引导到设置页
3. 设置页输入 Key 并保存 → 确认文件写入正确
4. 粘贴中文文本 → 点击润色 → 输出语序调整后的结果
5. 清空和复制功能正常
6. 窗口缩放自适应
